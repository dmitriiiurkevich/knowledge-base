*SQL* — *Structured Query Language* — язык структурированных запросов. Все команды *SQL* можно разделить на 5 групп:
- *DDL* — *Data Definition Language* — язык определения данных
	- CREATE —
	- DROP —
	- ALTER —
	- TRUNCATE —
	- COMMENT —
	- RENAME —
- DQL — *Data Query Language* — язык запроса данных
	- SELECT —
- *DML* — *Data Manipulation Language* — язык управления данными
	- INSERT —
	- UPDATE —
	- DELETE —
	- LOCK —
	- CALL —
	- EXPLAIN PLAN —
- *DCL* — *Data Control Language* — язык управления доступом к данным
	- GRANT —
	- REVOKE —
- *TCL* — *Transaction Control Language* — язык управления транзакциями
	- BEGIN TRANSACTION —
	- COMMT —
	- ROLLBACK —
	- SAVEPOINT —

https://www.geeksforgeeks.org/sql-ddl-dql-dml-dcl-tcl-commands/